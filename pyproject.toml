[project]
name = "paper-reading"
version = "0.1.0"
description = "Airborne high-resolution SAR literature demo backend scaffold"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn>=0.30.0",
  "pydantic>=2.7.0",
]

[tool.uv]
dev-dependencies = []
